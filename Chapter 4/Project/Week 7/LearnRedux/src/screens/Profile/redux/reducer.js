const initialState = {
  name: 'Yohanes Velly',
  age: 21,
  kriteria: {
    height: '178cm',
    weight: '65kg',
  },
};

export const profileReducer = (state = initialState, action) => {
  return state;
};
