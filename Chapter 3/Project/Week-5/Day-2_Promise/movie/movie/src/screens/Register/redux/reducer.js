const initialState = {
  title: 'Hi! Nice To See You',
  titleDesc: 'Register Your Account Below',
  button: 'Register',
};

export const registerReducer = (state = initialState, action) => {
  return state;
};
